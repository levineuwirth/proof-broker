# This file is generated by dune, edit dune-project instead
opam-version: "2.0"
synopsis: "Proof Broker SDK shared library (OCaml)"
description:
  "OCaml implementation of the Proof Broker SDK per spec v1.0. Hosts the IR rewriter, certificate verifier core, dispatcher, and cache. The Rocq plugin links this library directly; the Lean plugin invokes it via a C-FFI shim with serialized-data marshaling."
depends: [
  "ocaml" {>= "5.0"}
  "dune" {>= "3.21" & < "3.24"}
  "yojson" {>= "3.0"}
  "digestif" {>= "1.0"}
  "zarith" {>= "1.13"}
  "alcotest" {with-test}
  "odoc" {with-doc}
]
build: [
  ["dune" "subst"] {dev}
  [
    "dune"
    "build"
    "-p"
    name
    "-j"
    jobs
    "@install"
    "@runtest" {with-test}
    "@doc" {with-doc}
  ]
]
x-maintenance-intent: ["(latest)"]
