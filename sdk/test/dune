(tests
 (names test_round_trip test_propositional_simplify test_definition_unfolding
        test_quotient_elimination test_metadata test_manifest
        test_certificate test_pipeline test_linear_arith test_farkas
        test_smtlib test_adapter_cvc4 test_adapter_cvc5
        test_refinement test_dispatch test_alethe_farkas)
 (libraries proof_broker yojson alcotest str))

; test_round_trip resolves the reference IR fixtures (in repo/examples,
; outside the sdk/ dune workspace) at runtime relative to its build
; directory rather than declaring them as dune deps.
;
; test_propositional_simplify and test_pipeline work on synthetic IR
; fragments built in-process and need no fixture access.
;
; [str] is added for [test_pipeline]'s diagnostic-substring check.
